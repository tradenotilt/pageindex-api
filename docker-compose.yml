version: '3.8'

services:
  pageindex-api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: pageindex-api
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./results:/app/results
    restart: unless-stopped
